# CHANGELOG

## v1.2.10

- `/老婆次数` 改为使用 `Tools.py` 的 `发送回复文本` 方法发送结果，保持回复格式一致。

## v1.2.9

- `/老婆次数` 查询结果新增当前用户名称和 QQ 号。

## v1.2.8

- 新增 `/老婆次数` 指令，可查询当前用户今日分手、许愿、强娶的已用次数与剩余次数。
- `/老婆菜单` 与 README 同步补充 `/老婆次数` 说明。
